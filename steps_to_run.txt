To run this map reduce system through shell:






Before running this project, make sure the Golang environment is setup:


To run this map reduce system through shell:
0) Golang environment has to be installed.




# cd to the root ...../CS545_V2.0, export 


0)open a new shell window, cd to src/master
For 

1)open another shell window, 